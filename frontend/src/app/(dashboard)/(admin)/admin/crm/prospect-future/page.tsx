import type { Metadata } from "next";
import ProspectFuture from "./ProspectFuture";

export const metadata: Metadata = {
  title: "Future Prospects",
};

export default function Page() {
  return <ProspectFuture />;
}
